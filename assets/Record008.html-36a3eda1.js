import{_ as a,W as s,X as e,Y as n}from"./framework-6447176f.js";const i={},l=n(`<h1 id="linux-安装java环境" tabindex="-1"><a class="header-anchor" href="#linux-安装java环境" aria-hidden="true">#</a> linux 安装java环境</h1><p>先确认安装情况</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">java</span> <span class="token parameter variable">--version</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>创建目录</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> /usr/java
<span class="token builtin class-name">cd</span> /usr/java
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>下载 JDK</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">wget</span> https://repo.huaweicloud.com/java/jdk/8u201-b09/jdk-8u201-linux-x64.tar.gz

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>将 JDK 源码包解压</p><p>上一步已经新建了安装目录并且将源码包拷贝到了新目录下，因为是压缩包，因此首先需要对其进行解压操作。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code> <span class="token function">tar</span> <span class="token parameter variable">-zxvf</span> jdk-8u201-linux-x64.tar.gz 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>打开 profile 文件</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code> <span class="token function">vim</span> /etc/profile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>打开这个文件之后，按 i 进入到编辑模式，然后移动到最后一行，添加如下语句</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">export</span> <span class="token assign-left variable">JAVA_HOME</span><span class="token operator">=</span>/usr/local/java/jdk1.8
<span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token variable">$JAVA_HOME</span>/bin:<span class="token environment constant">$PATH</span>
<span class="token builtin class-name">export</span> <span class="token assign-left variable">CLASSPATH</span><span class="token operator">=</span>.:<span class="token variable">$JAVA_HOME</span>/lib/dt.jar:<span class="token variable">$JAVA_HOME</span>/lib/tools.jar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>添加完成之后，然后按 Esc，输入 :wq，保存修改并退出。</p><p>重新读取环境变量 执行到这里，我么已经配置了 Java 的环境变量，那么现在需要做的就是让它生效，这里我们不必重启机器让其生效，通过 source 命令就可以。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token builtin class-name">source</span> /etc/profile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>之后，刚刚配置的环境变量便会生效，原理就是重新读取以下 profile 配置文件中的内容。</p><p>确认 JDK 安装情况</p><p>上面已经提到如何确认 JDK 是否成功安装，只需要通过查看版本即可看到。那么同样的，这里依旧采用同样的方法来验证 JDK 的安装情况。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code> <span class="token function">java</span> <span class="token parameter variable">-version</span>

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,21),d=[l];function r(c,t){return s(),e("div",null,d)}const o=a(i,[["render",r],["__file","Record008.html.vue"]]);export{o as default};
